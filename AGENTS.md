# RaidAttack — agent guide

See [`CLAUDE.md`](CLAUDE.md) for the full, authoritative guide (stack, plugin layout, server
config, and the build → deploy → restart workflow). This file intentionally just points there
so the two don't drift out of sync.
